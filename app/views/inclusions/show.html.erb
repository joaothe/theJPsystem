<p>
  <strong>Title:</strong>
  <%= @inclusion.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @inclusion.text %>
</p>

<h2>Comments</h2>
<%= render @inclusion.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<td><%= link_to 'Edit', edit_inclusion_path(@inclusion) %></td>

<%= link_to 'Back' , inclusions_path %>
